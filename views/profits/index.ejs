<%- include('../partials/header') %>
<table class="table table-striped table-dark" id="list">
    <thead>
        <tr>
            <th>Week</th>
            <th>profits</th>

        </tr>
    </thead>
    <form d="add-profit-form" method="POST" action="/profits">
        <input type="text" name="week" placeholder="week" />
        <input type="number" name="profits" placeholder="profits" />

        <input type="submit" value="Add" />
    </form>
    <tbody>
        <% traders.forEach(function(trader) { %>
        <% trader.profits.forEach(function(u) { %>
        <tr>
            <td><%= u.week %></td>
            <td><%= u.profits %></td>



            <td>
                <% if (trader._id.equals(user && user._id)) { %>
                <form action="/profits/<%= u._id %>?_method=DELETE" method="POST" class="secondary-content">
                    <button type="submit">X</button>
                </form>
                <% } %>

                </form>
            </td>
        </tr>
        <% }); %>
        <% }); %>
    </tbody>
</table>
<%- include('../partials/footer') %>